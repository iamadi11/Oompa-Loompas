---
description: Fully autonomous feature lifecycle — AI decides what to build next, acts as PM + BA + architect + dev + QA + devops + brutal critic user
---

$ARGUMENTS

Invoke the `ship` skill. Run it fully autonomously — no human input unless genuinely blocked.
